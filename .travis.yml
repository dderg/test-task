language: node_js
sudo: false
node_js:
    - "4.1"

before_install:
    - "apt-get update"
    - "apt-get install redis-server"
    - "redis-server /etc/redis/redis.conf"

before_script:
    - "npm install -g mocha"
